import type { AppModuleManifest } from '@/modules/core/types'

export const updatesModule: AppModuleManifest = {
  id: 'updates',
  title: 'Updates',
  description: 'Starter updates subscription capability for the public landing experience.',
  enabledByDefault: false,
  routes: [],
}
